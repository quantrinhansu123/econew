# Production Vercel + Backend Render (repo: github.com/quantrinhansu123/econew)
# VITE_API_URL=https://eco-transport-api.onrender.com/api/v1

# Dev local: KHÔNG bật direct — dùng Vite proxy /api → backend (đọc PORT từ server/.env)
# VITE_API_URL_DIRECT=true
