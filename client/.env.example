# Production: backend Render (mặc định trong code nếu không set)
# VITE_API_URL=https://econew.onrender.com/api/v1
# Dev local: KHÔNG bật direct — dùng Vite proxy /api → backend (đọc PORT từ server/.env)
# VITE_API_URL_DIRECT=true
